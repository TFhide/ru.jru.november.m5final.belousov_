spring.cache.type: none

spring:
  datasource:
    url: jdbc:h2:mem:testdb
  h2:
    console:
      enabled: true
  sql:
    init:
      mode: always
      platform: h2

  liquibase:
    changeLog: "classpath:schema/changelogTest.sql"


